import 'package:flutter/material.dart';

class JobCreateScreen extends StatelessWidget {
  const JobCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Job Create'),
      ),
    );
  }
}
